name: flip_widgets
description: A Flutter package for creating customizable flip animations between two widgets.
version: 0.0.1
homepage: https://github.com/iamtvirani/flip_widgets
repository: https://github.com/iamtvirani/flip_widgets

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
